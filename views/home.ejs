<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include('partials/head') %>
    <title>Home</title>
  </head>
  <body>
    <%- include('partials/nav') %>
    <div class="container mx-auto p-4">
      <h1 class="text-3xl font-bold mb-6">My Blogs</h1>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <% if (locals.user && locals.blogs && blogs.length > 0) { %> <%
        blogs.forEach(blog => { %>
        <div
          class="bg-white dark:bg-gray-800 shadow-lg rounded-lg overflow-hidden"
        >
          <% if (blog.coverImageURL) { %>
          <img
            src="<%= blog.coverImageURL %>"
            alt="<%= blog.title %>"
            class="w-full h-auto rounded-lg mb-4"
          />
          <% } %>
          <div class="p-6">
            <h2
              class="text-xl font-semibold text-gray-900 dark:text-white mb-2"
            >
              <%= blog.title %>
            </h2>
            <p class="text-gray-700 dark:text-gray-300 text-sm mb-4">
              <%= blog.body.substring(0, 100) %>...
            </p>
            <a
              href="/blog/<%= blog._id %>"
              class="inline-block bg-indigo-600 text-white text-sm font-medium px-4 py-2 rounded-md hover:bg-indigo-700"
              >Read More</a
            >
          </div>
        </div>
        <% }) %> <% } else { %>
        <p class="text-gray-700 dark:text-gray-300 col-span-full">
          No blogs found. Start by creating a new one!
        </p>
        <% } %>
      </div>
    </div>
  </body>
</html>
